# =============================================================================
# ETF Investment Application - Dependencies
# =============================================================================

# Data Ingestion
fredapi>=0.5.0
yfinance>=0.2.18
finnhub-python>=2.4.18
nasdaq-data-link>=1.0.4

# Cloud persistence (daily-refreshed caches survive Cloud Run scale-to-zero)
google-cloud-storage>=2.10.0

# Regime Detection
hmmlearn>=0.3.0
scikit-learn>=1.3.0
statsmodels>=0.14.0
arch>=6.0.0

# Portfolio Optimization
PyPortfolioOpt>=1.5.5
riskfolio-lib>=4.0.0

# Backtesting
bt>=0.2.9
vectorbt>=0.26.0
QuantStats>=0.0.62

# Execution
ib_async>=1.0.0

# Dashboard
dash>=2.14.0
dash-bootstrap-components>=1.5.0
plotly>=5.18.0

# NLP Sentiment (FinBERT). torch (CPU) is installed separately in the Dockerfile
# to keep the wheel small. If torch is absent (e.g. local dev), the sentiment
# analyzer falls back to a finance lexicon automatically.
transformers>=4.36.0
# Social-media-tuned sentiment (Reddit retail panel) — tiny, pure-python.
vaderSentiment>=3.3.2

# Core
pandas>=2.1.0
numpy>=1.24.0
scipy>=1.11.0

# Utilities
python-dotenv>=1.0.0
APScheduler>=3.10.0
requests>=2.31.0
aiohttp>=3.9.0
